<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>CamusDB Blog</title>
        <link>https://camusdb.github.io/blog/</link>
        <description>CamusDB Blog</description>
        <lastBuildDate>Wed, 19 Aug 2026 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <language>en</language>
        <item>
            <title><![CDATA[We Built A Dragon To Attack Our Own Database]]></title>
            <link>https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/</link>
            <guid>https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/</guid>
            <pubDate>Wed, 19 Aug 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[A database earns your trust on its worst day, not on its best one.]]></description>
            <content:encoded><![CDATA[<p>A database earns your trust on its worst day, not on its best one.</p>
<p>When everything is healthy, almost any database looks correct. You insert rows,
you read them back, and the numbers add up. The real question is what happens
when a server dies in the middle of a commit, when the network starts losing
packets, or when a disk fills up while a transaction is still writing.</p>
<p>Those moments are hard to create by hand. They are also the moments a
distributed database exists to survive.</p>
<p>So we built a tool that creates them whenever we want. We named it Caraxes,
after a dragon, because its only job is to attack our own cluster while we
watch what happens.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="one-server-or-many-is-a-very-different-problem">One Server Or Many Is A Very Different Problem<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#one-server-or-many-is-a-very-different-problem" class="hash-link" aria-label="Direct link to One Server Or Many Is A Very Different Problem" title="Direct link to One Server Or Many Is A Very Different Problem" translate="no">​</a></h2>
<p>A database on a single server has a simple failure story. The process is either
running or it is not. When it stops, everyone notices at the same time, and
recovery means one thing: read the write-ahead log and continue.</p>
<p>A cluster is not that kind. In a cluster, one part can break while everything
else keeps working. Even better, the parts that still work often cannot tell
what kind of break they are looking at.</p>
<p>Imagine three nodes, and one of them goes quiet. From outside, that looks the
same as:</p>
<ul>
<li class="">the node crashed,</li>
<li class="">the node is alive but frozen for a few seconds,</li>
<li class="">the node is alive and fine, but the network between it and the others is
dropping packets.</li>
</ul>
<p>Each case needs a different reaction. A crashed node must be replaced. A frozen
node will wake up by itself. A node behind a broken network may still think it
is the leader, and may still be accepting work.</p>
<p>The cluster has to choose a reaction anyway. It has to choose quickly, with
missing information, while people are waiting for their queries. Every
distributed database is a set of answers to that problem, and those answers are
only as good as the conditions you tested them in.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="failing-loudly-is-fine-failing-quietly-is-not">Failing Loudly Is Fine. Failing Quietly Is Not.<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#failing-loudly-is-fine-failing-quietly-is-not" class="hash-link" aria-label="Direct link to Failing Loudly Is Fine. Failing Quietly Is Not." title="Direct link to Failing Loudly Is Fine. Failing Quietly Is Not." translate="no">​</a></h2>
<p>A database that returns an error is annoying. A database that returns a wrong
answer is dangerous, because nobody looks for a problem they were never told
about.</p>
<p>When something breaks, the danger changes shape. It stops being about speed and
starts being about truth:</p>
<ul>
<li class="">a write is confirmed, then lost when a new leader takes over,</li>
<li class="">a deleted row comes back after an old replica is promoted,</li>
<li class="">a transfer takes money from one account and never adds it to the other.</li>
</ul>
<p>A normal test suite will not find any of that. A normal test suite runs on one
healthy machine, with plenty of disk and a perfect network. Those tests are
still worth having. They simply cannot answer the questions that matter most in
cluster mode.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-reliability-really-means">What Reliability Really Means<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#what-reliability-really-means" class="hash-link" aria-label="Direct link to What Reliability Really Means" title="Direct link to What Reliability Really Means" translate="no">​</a></h2>
<p>Reliability is not one thing. It is several, and each one needs its own proof.</p>
<ul>
<li class="">Durability. If we confirm your commit, it survives, even if the node that
confirmed it dies one second later.</li>
<li class="">Consistency. You never see half of a transaction, no matter which replica
answers your query.</li>
<li class="">Availability. The cluster keeps serving while part of it is down.</li>
<li class="">Recovery. After the problem is fixed, the cluster goes back to normal by
itself, and fast.</li>
<li class="">Predictability. The same failure behaves the same way every time.</li>
</ul>
<p>A database can pass four of these and fail the fifth, and still be painful to
run. Imagine a cluster that stays perfectly correct but needs two minutes to
answer queries again after losing one node. It is correct, and it is still a bad
afternoon for everyone waiting.</p>
<p>This is why Caraxes measures all of them in a single run.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-caraxes-works">How Caraxes Works<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#how-caraxes-works" class="hash-link" aria-label="Direct link to How Caraxes Works" title="Direct link to How Caraxes Works" translate="no">​</a></h2>
<p>A test is one YAML file. It says which cluster to build, what load to run, what
to break, and what result we expect.</p>
<div class="language-yml codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-yml codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token key atrule" style="color:#00a4db">name</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> kill</span><span class="token punctuation" style="color:#393A34">-</span><span class="token plain">follower</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token key atrule" style="color:#00a4db">cluster</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">name</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> chaos</span><span class="token punctuation" style="color:#393A34">-</span><span class="token plain">kill</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">nodes</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">3</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">partitions</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">3</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">replication_factor</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">3</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token key atrule" style="color:#00a4db">workload</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">database</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> caraxes</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">rows</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">20000</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">mode</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> open</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">target_ops</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">300</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">workers</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">32</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">duration</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> 60s</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">warmup</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> 15s</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token key atrule" style="color:#00a4db">nemesis</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">seed</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">7</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">events</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">    </span><span class="token punctuation" style="color:#393A34">-</span><span class="token plain"> </span><span class="token punctuation" style="color:#393A34">{</span><span class="token plain"> </span><span class="token key atrule" style="color:#00a4db">at</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> 20s</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"> </span><span class="token key atrule" style="color:#00a4db">fault</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> kill</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"> </span><span class="token key atrule" style="color:#00a4db">target</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> random</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"> </span><span class="token key atrule" style="color:#00a4db">duration</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> 20s </span><span class="token punctuation" style="color:#393A34">}</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token key atrule" style="color:#00a4db">checks</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  </span><span class="token key atrule" style="color:#00a4db">max_recovery_seconds</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token number" style="color:#36acaa">45</span><br></div></code></pre></div></div>
<p>One command does the rest. It builds the CamusDB image from the code on your
disk, starts a real cluster in Docker, waits until every node is ready, loads a
dataset, and sends real SQL traffic through the normal client. Twenty seconds
in, it kills a node. Twenty seconds later, it brings the node back.</p>
<p>The failures are real, not simulated:</p>
<ul>
<li class="">a kill is a real SIGKILL to the process,</li>
<li class="">a network partition is a real firewall rule that drops packets between nodes,</li>
<li class="">a slow link is real added latency on the network interface,</li>
<li class="">a full disk is a real file that grows until the disk has no space left.</li>
</ul>
<p>We avoid simulations here. A simulation only tests the behavior we already
imagined.</p>
<p>At the end, the run finishes with a pass or a fail, so a machine can read the
result without a human in the loop.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-simple-rule-beats-a-long-discussion">A Simple Rule Beats A Long Discussion<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#a-simple-rule-beats-a-long-discussion" class="hash-link" aria-label="Direct link to A Simple Rule Beats A Long Discussion" title="Direct link to A Simple Rule Beats A Long Discussion" translate="no">​</a></h2>
<p>The strongest check in the whole tool is also the easiest one to explain.</p>
<p>One of our test workloads moves money between random accounts. Every transfer
takes an amount from one row and adds it to another, inside one transaction.</p>
<p>That gives us a rule that cannot be argued with. Money only moves, it is never
created or destroyed, so the total of all balances must be exactly the same
after the test as before it.</p>
<p>Now we do not need to read logs or discuss whether a warning was serious. If the
total changed, a transaction was not atomic, and no amount of good performance
makes that acceptable. We never ignore this check, not even in a test where we
killed a node on purpose.</p>
<p>These transfers also create real conflicts between transactions, which is the
whole idea. Our other workload avoids conflicts so it can measure speed and
failover cleanly, but a test without conflicts will never catch a broken
transaction. You have to make transactions fight over the same rows, and then
kill a node while they are fighting.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-same-error-can-mean-two-things">The Same Error Can Mean Two Things<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#the-same-error-can-mean-two-things" class="hash-link" aria-label="Direct link to The Same Error Can Mean Two Things" title="Direct link to The Same Error Can Mean Two Things" translate="no">​</a></h2>
<p>An error means something different depending on what was happening at the time.</p>
<p>If we injected no failure at all, an internal error has no excuse. Nothing was
attacking the cluster, so we treat it as a real bug and the test fails.</p>
<p>If we just killed a node, the situation is different. Connections are closing
while requests are still travelling, so a few client-side errors are normal.
We count them and print them clearly, but they alone do not fail a test that
stayed correct and recovered in time.</p>
<p>This balance matters more than it sounds. A test that reports a problem every
single time gets ignored, and an ignored test protects nothing.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="recovery-time-is-a-number-we-watch">Recovery Time Is A Number We Watch<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#recovery-time-is-a-number-we-watch" class="hash-link" aria-label="Direct link to Recovery Time Is A Number We Watch" title="Direct link to Recovery Time Is A Number We Watch" translate="no">​</a></h2>
<p>While the failures happen, Caraxes writes down every break and every repair,
with exact timestamps. At the same time, the workload records what happened each
second: how many operations succeeded, how many failed, and how slow they were.</p>
<p>Afterwards, we put both records on the same clock. That answers three questions
for every failure. How bad did it get? Did the cluster keep doing useful work,
or did it stop completely? And how many seconds after the repair did things go
back to normal?</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain"># Fault analysis</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">- Normal seconds: error rate 0.00 %, write p99 1,382.0 ms</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">- During the failure: error rate 40.45 %, write p99 392.9 ms</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">- Longest recovery: 5.5 s; everything recovered: yes</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">| fault | healed | window (s) | peak error | failed | progressed | recovery (s) |</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">|---|---|---|---|---|---|---|</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">| kill/camus2 | yes | 20.3 | 100 % | 4,074 | yes | 5.5 |</span><br></div></code></pre></div></div>
<p>Reading that line: we killed a node, errors jumped, the cluster never stopped
completely, and five and a half seconds after the node came back everything was
normal again. That last number is what we compare from one build to the next.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="every-test-can-be-repeated">Every Test Can Be Repeated<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#every-test-can-be-repeated" class="hash-link" aria-label="Direct link to Every Test Can Be Repeated" title="Direct link to Every Test Can Be Repeated" translate="no">​</a></h2>
<p>Every random choice comes from a seed number. The same seed kills the same node
at the same second, in the same order.</p>
<p>This is what turns a demo into a test. When a run fails, we can run it again and
watch it fail in exactly the same way. After we fix the cause, we run it once
more and watch it pass. A failure you cannot reproduce is just a story you tell
later.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-we-break-today">What We Break Today<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#what-we-break-today" class="hash-link" aria-label="Direct link to What We Break Today" title="Direct link to What We Break Today" translate="no">​</a></h2>
<p>Each test asks one clear question.</p>
<ul>
<li class="">A run with no failures at all, so we know the cluster and the traffic work
before we break anything.</li>
<li class="">A node killed in the middle of the run, then restarted.</li>
<li class="">A node cut off from all the others, and separately a node slowed down, so
nothing dies but the network stops cooperating.</li>
<li class="">Money transfers running while a node is killed, with the total checked
afterwards.</li>
<li class="">Six nodes in three zones, with one entire zone killed, so every partition
loses a replica at the same moment and has to survive with two.</li>
<li class="">A node with a full disk, to confirm it refuses writes politely instead of
crashing, and starts accepting them again once there is space.</li>
<li class="">A long run with random failures arriving every ten to eighteen seconds.</li>
<li class="">A test for the leader balancer alone: kill the busiest node, restart it, and
check that leadership actually comes back to it.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="stopping-problems-from-coming-back">Stopping Problems From Coming Back<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#stopping-problems-from-coming-back" class="hash-link" aria-label="Direct link to Stopping Problems From Coming Back" title="Direct link to Stopping Problems From Coming Back" translate="no">​</a></h2>
<p>A quality that nobody measures slowly gets worse.</p>
<p>Nobody breaks recovery time on purpose. Someone adds a cache, changes a timeout,
or moves two lines in the commit path, and a fifteen second recovery becomes
fifty. Each change looks fine in review. All the unit tests still pass, because
none of them ever unplugged a server.</p>
<p>That is why we can run a whole grid of tests at once. One file expands into many
runs across locking modes, cluster sizes, and failure types, and produces a
table with every result and every recovery time side by side. When a change
costs us reliability, it shows up as a row that used to pass and now does not.</p>
<p>The result is a pass or fail code that CI can read, because a judgement that
lives only in someone's memory of last month is not protection.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-comes-next">What Comes Next<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#what-comes-next" class="hash-link" aria-label="Direct link to What Comes Next" title="Direct link to What Comes Next" translate="no">​</a></h2>
<p>The plan is to keep adding tests until the questions we care about all have
answers.</p>
<ul>
<li class="">Deeper correctness checking, to catch strange orderings that a simple total
cannot detect.</li>
<li class="">Disks that are slow or that return damaged data, not only disks that are full.</li>
<li class="">Clocks that disagree between nodes, because time is one of the things a
cluster trusts the most.</li>
<li class="">Nodes joining and leaving while traffic is running.</li>
<li class="">Schema changes during failures, since a migration is the worst possible moment
to lose a node.</li>
<li class="">Distributed queries during failures, so a query split across the cluster still
returns the same rows when one part disappears.</li>
<li class="">Backups and restores while the cluster is under attack.</li>
<li class="">Bigger clusters and much longer runs, because some bugs only appear after
hours.</li>
</ul>
<p>Every item is a question with a yes or no answer and a number next to it. We are
not collecting a badge that says the software passed. We are building a list of
questions we can answer with evidence instead of hope.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-this-matters-to-us">Why This Matters To Us<a href="https://camusdb.github.io/blog/a-dragon-to-attack-our-own-database/#why-this-matters-to-us" class="hash-link" aria-label="Direct link to Why This Matters To Us" title="Direct link to Why This Matters To Us" translate="no">​</a></h2>
<p>CamusDB is meant to run in cluster mode for people who will never read its
source code.</p>
<p>They will not know which failures we thought about, which ones we tested, and
which ones we only assumed would be fine. They will find out on the day a server
dies during a busy afternoon. On that day, they will judge us on two things
only: was the data still there, and did the application keep working?</p>
<p>Preparing for that afternoon before it arrives is the only honest way to deserve
that trust. Chaos testing does not prove a database is perfect. It makes sure
that when it does fail, it fails in a way we already studied, already measured,
and already decided we can accept.</p>]]></content:encoded>
            <category>camusdb</category>
            <category>reliability</category>
            <category>chaos-testing</category>
            <category>distributed-systems</category>
        </item>
        <item>
            <title><![CDATA[When Every Experiment Needs Its Own Database]]></title>
            <link>https://camusdb.github.io/blog/database-branches-for-ai-agents/</link>
            <guid>https://camusdb.github.io/blog/database-branches-for-ai-agents/</guid>
            <pubDate>Tue, 28 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Software is starting to work at a different rhythm.]]></description>
            <content:encoded><![CDATA[<p>Software is starting to work at a different rhythm.</p>
<p>A developer may test one idea at a time, but an AI agent can explore several
possible solutions in parallel. One path may try a schema migration. Another
may rewrite some data. A third may reproduce a bug using a different sequence
of events.</p>
<p>Each path needs a database that feels real, but none of them should be allowed
to damage the shared starting point.</p>
<p>This is where database branching becomes more than a convenient development
feature. It becomes a practical way to give every experiment a safe place to
work.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="one-starting-point-many-possible-futures">One Starting Point, Many Possible Futures<a href="https://camusdb.github.io/blog/database-branches-for-ai-agents/#one-starting-point-many-possible-futures" class="hash-link" aria-label="Direct link to One Starting Point, Many Possible Futures" title="Direct link to One Starting Point, Many Possible Futures" translate="no">​</a></h2>
<p>Imagine that an agent is asked to improve an online store's checkout flow. It
may need to add a column, update existing orders, create an index, and test how
the application behaves with the new structure.</p>
<p>There is rarely only one possible solution. The agent might want to compare
three approaches:</p>
<ul>
<li class="">Add a new status to the current table.</li>
<li class="">Move payment details into a separate table.</li>
<li class="">Keep the schema and change only the application logic.</li>
</ul>
<p>If all three approaches use the same database, they will interfere with one
another. A migration from the first attempt may break the second. Test data
from the second may change the result of the third. When something fails, it
becomes difficult to tell which action caused the problem.</p>
<p>The usual response is to create separate databases or restore the same backup
several times. That can work, but it adds time and operational work. The cost
becomes much more visible when experiments are created by software, not only by
people.</p>
<p>An agent that can explore ten ideas should not spend most of its time waiting
for ten full database copies.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-branch-is-a-private-timeline">A Branch Is A Private Timeline<a href="https://camusdb.github.io/blog/database-branches-for-ai-agents/#a-branch-is-a-private-timeline" class="hash-link" aria-label="Direct link to A Branch Is A Private Timeline" title="Direct link to A Branch Is A Private Timeline" translate="no">​</a></h2>
<p>A database branch starts from a known moment in another database. At the
beginning, it sees the same schema and data. After that, it follows its own
path.</p>
<p>Changes in the branch stay in the branch:</p>
<ul>
<li class="">New rows do not appear in the parent database.</li>
<li class="">Updates do not replace the parent's values.</li>
<li class="">Deletes do not remove the parent's rows.</li>
<li class="">Schema changes do not alter the parent's structure.</li>
</ul>
<p>This gives each experiment a private timeline. The agent can make bold changes
because the original database remains untouched.</p>
<p>The idea is similar to working on a separate code branch, but data introduces
its own challenges. A database is often much larger than a source repository,
and it changes while applications are running. Creating a branch must preserve
a consistent view without turning every experiment into a full copy of all
stored rows.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-creation-time-matters">Why Creation Time Matters<a href="https://camusdb.github.io/blog/database-branches-for-ai-agents/#why-creation-time-matters" class="hash-link" aria-label="Direct link to Why Creation Time Matters" title="Direct link to Why Creation Time Matters" translate="no">​</a></h2>
<p>Branching is useful only when it is cheap enough to become part of the normal
workflow.</p>
<p>If creating an isolated environment takes several minutes, an agent will avoid
doing it frequently. It may reuse an old test database, perform fewer
experiments, or place several tasks in the same environment. All of those
choices reduce isolation.</p>
<p>When branches can be created quickly, the safer option also becomes the
simpler option. A system can create a fresh branch for every task, attempt, or
test run. Failed branches can be inspected and then removed. Successful
branches can provide evidence for the next step.</p>
<p>Creation time also affects parallel work. Suppose one task explores eight
possible changes. The database should not become the slow gate in front of
those eight workers. The value comes from allowing them to start from the same
state and move independently.</p>
<p>In other words, branch speed is not just about saving a few seconds. It
determines whether branching is an occasional manual action or a basic building
block for automated work.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-explore-evaluate-discard-loop">The Explore, Evaluate, Discard Loop<a href="https://camusdb.github.io/blog/database-branches-for-ai-agents/#the-explore-evaluate-discard-loop" class="hash-link" aria-label="Direct link to The Explore, Evaluate, Discard Loop" title="Direct link to The Explore, Evaluate, Discard Loop" translate="no">​</a></h2>
<p>Many agent workflows follow a simple pattern:</p>
<ol>
<li class="">Start from a known state.</li>
<li class="">Create one or more isolated paths.</li>
<li class="">Make a different change in each path.</li>
<li class="">Run checks and compare the results.</li>
<li class="">Keep the useful information and discard the temporary environments.</li>
</ol>
<p>The database needs to support this loop without making every round expensive.</p>
<p>Consider an agent investigating a production-only bug. It can create several
branches from the same point in time. In one branch, it changes an order's
status. In another, it removes a related payment record. In a third, it applies
a proposed schema fix. Because every branch has the same starting point, the
results are easier to compare.</p>
<p>This approach is also useful for:</p>
<ul>
<li class="">Rehearsing migrations against realistic data.</li>
<li class="">Testing data repair scripts before approval.</li>
<li class="">Comparing alternative indexing strategies.</li>
<li class="">Generating test cases that modify the database.</li>
<li class="">Running destructive checks in disposable environments.</li>
</ul>
<p>The important part is not that an agent can write SQL. The important part is
that it can try, observe, and fail without leaving a confusing trail in a
shared database.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-camusdb-approaches-branching">How CamusDB Approaches Branching<a href="https://camusdb.github.io/blog/database-branches-for-ai-agents/#how-camusdb-approaches-branching" class="hash-link" aria-label="Direct link to How CamusDB Approaches Branching" title="Direct link to How CamusDB Approaches Branching" translate="no">​</a></h2>
<p>CamusDB creates an isolated, point-in-time branch with SQL:</p>
<div class="language-camussql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-camussql codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token keyword" style="color:#00009f">CREATE</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">DATABASE</span><span class="token plain"> checkout_attempt </span><span class="token keyword" style="color:#00009f">BRANCH</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">FROM</span><span class="token plain"> prod</span><span class="token punctuation" style="color:#393A34">;</span><br></div></code></pre></div></div>
<p>The new database begins with the schema and data view from <code>prod</code> at the fork
point. CamusDB does not copy every source row when the branch is created.
Instead, inherited reads use the source snapshot, while new writes and schema
changes go into a private overlay owned by the branch.</p>
<p>That copy-on-write model is important. A branch can begin from a large,
realistic database without first duplicating all of its contents. Storage grows
as the branch diverges, not simply because the branch exists.</p>
<p>For example, two experiments can start from the same database:</p>
<div class="language-camussql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-camussql codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token keyword" style="color:#00009f">CREATE</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">DATABASE</span><span class="token plain"> checkout_schema_test </span><span class="token keyword" style="color:#00009f">BRANCH</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">FROM</span><span class="token plain"> prod</span><span class="token punctuation" style="color:#393A34">;</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">CREATE</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">DATABASE</span><span class="token plain"> checkout_data_repair </span><span class="token keyword" style="color:#00009f">BRANCH</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">FROM</span><span class="token plain"> prod</span><span class="token punctuation" style="color:#393A34">;</span><br></div></code></pre></div></div>
<p>The first branch can test a migration:</p>
<div class="language-camussql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-camussql codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token keyword" style="color:#00009f">USE</span><span class="token plain"> checkout_schema_test</span><span class="token punctuation" style="color:#393A34">;</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">ALTER</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">TABLE</span><span class="token plain"> orders </span><span class="token keyword" style="color:#00009f">ADD</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">COLUMN</span><span class="token plain"> payment_reference </span><span class="token builtin class-name">STRING</span><span class="token punctuation" style="color:#393A34">;</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">CREATE</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">INDEX</span><span class="token plain"> orders_payment_reference_idx</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">ON</span><span class="token plain"> orders </span><span class="token punctuation" style="color:#393A34">(</span><span class="token plain">payment_reference</span><span class="token punctuation" style="color:#393A34">)</span><span class="token punctuation" style="color:#393A34">;</span><br></div></code></pre></div></div>
<p>The second can test a data change:</p>
<div class="language-camussql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-camussql codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token keyword" style="color:#00009f">USE</span><span class="token plain"> checkout_data_repair</span><span class="token punctuation" style="color:#393A34">;</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain" style="display:inline-block"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">UPDATE</span><span class="token plain"> orders</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">SET</span><span class="token plain"> status </span><span class="token operator" style="color:#393A34">=</span><span class="token plain"> </span><span class="token string" style="color:#e3116c">"review"</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">WHERE</span><span class="token plain"> status </span><span class="token operator" style="color:#393A34">=</span><span class="token plain"> </span><span class="token string" style="color:#e3116c">"payment_unknown"</span><span class="token punctuation" style="color:#393A34">;</span><br></div></code></pre></div></div>
<p>Neither experiment changes <code>prod</code>, and neither branch can see the other
branch's work.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="isolation-makes-results-easier-to-trust">Isolation Makes Results Easier To Trust<a href="https://camusdb.github.io/blog/database-branches-for-ai-agents/#isolation-makes-results-easier-to-trust" class="hash-link" aria-label="Direct link to Isolation Makes Results Easier To Trust" title="Direct link to Isolation Makes Results Easier To Trust" translate="no">​</a></h2>
<p>Parallel execution is only helpful when the results remain understandable.</p>
<p>When several workers share one mutable database, a successful test may depend
on data created by another worker. A failed test may be caused by an unrelated
migration. Retrying the same task can produce a different answer because the
starting state has already changed.</p>
<p>Branches reduce this uncertainty. Each attempt has a clear parent and a clear
fork point. That makes the database state part of the experiment's identity.
If an attempt fails, the branch can remain available for inspection. A
developer can query it, examine its schema, and follow the exact state that
produced the failure.</p>
<p>This is valuable for people as well as agents. Automation should not turn a
failure into a mystery. It should leave behind enough context for someone to
understand what happened.</p>
<p>CamusDB provides <code>SHOW BRANCHES</code> and <code>SHOW ANCESTORS</code> to inspect these
relationships:</p>
<div class="language-camussql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-camussql codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token keyword" style="color:#00009f">SHOW</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">BRANCHES</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">FROM</span><span class="token plain"> prod</span><span class="token punctuation" style="color:#393A34">;</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">SHOW</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">ANCESTORS</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">FROM</span><span class="token plain"> checkout_schema_test</span><span class="token punctuation" style="color:#393A34">;</span><br></div></code></pre></div></div>
<p>These commands help answer simple but important questions: Which experiments
still exist? Where did a branch come from? How deep is its history?</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="disposable-does-not-mean-careless">Disposable Does Not Mean Careless<a href="https://camusdb.github.io/blog/database-branches-for-ai-agents/#disposable-does-not-mean-careless" class="hash-link" aria-label="Direct link to Disposable Does Not Mean Careless" title="Direct link to Disposable Does Not Mean Careless" translate="no">​</a></h2>
<p>Temporary branches still have a cost.</p>
<p>A live CamusDB branch keeps its source snapshot available, so old history
cannot always be reclaimed while the branch depends on it. Deep chains also
require more work during reads because the database may need to check several
ancestors.</p>
<p>For automated workflows, a good rule is to keep branches focused and
short-lived:</p>
<ul>
<li class="">Create a branch for a specific task.</li>
<li class="">Record the result and any useful diagnostics.</li>
<li class="">Keep failed branches only when they need investigation.</li>
<li class="">Drop branches when the task is complete.</li>
<li class="">Avoid long chains when several branches can start from the same parent.</li>
</ul>
<p>This lifecycle matters at scale. Fast creation should be matched by clear
cleanup.</p>
<p>CamusDB also has an important limit today: branches are isolated workspaces,
not a merge system. Changes cannot currently be merged back into the parent,
and transactions cannot cross branches. A successful experiment should
produce a reviewed migration, script, or application change that can be
applied through the normal delivery process.</p>
<p>That boundary is useful for safety. An experimental branch is evidence, not
automatic permission to change the main database.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-better-default-for-automated-work">A Better Default For Automated Work<a href="https://camusdb.github.io/blog/database-branches-for-ai-agents/#a-better-default-for-automated-work" class="hash-link" aria-label="Direct link to A Better Default For Automated Work" title="Direct link to A Better Default For Automated Work" translate="no">​</a></h2>
<p>As software takes on more independent tasks, databases need to support more
than one shared present. They need to represent several possible futures at the
same time.</p>
<p>Database branching offers a clean model:</p>
<ul>
<li class="">The parent is the trusted starting point.</li>
<li class="">Each branch is an isolated proposal.</li>
<li class="">Tests decide which proposal is useful.</li>
<li class="">Unsuccessful paths can be removed without repairing the parent.</li>
</ul>
<p>For newcomers, the main idea is simple: give every experiment its own database
view, and make that view cheap enough to use by default.</p>
<p>This is the direction CamusDB is exploring with copy-on-write branches. The
feature is still evolving, like the rest of the project, but it already
captures an important principle: safe experimentation should be part of the
database itself, not a fragile process built around it.</p>
<p>When creating a clean starting point is easy, both developers and agents can
move faster without giving up control.</p>]]></content:encoded>
            <category>camusdb</category>
            <category>database-branching</category>
            <category>ai-agents</category>
        </item>
        <item>
            <title><![CDATA[Teaching CamusDB To Choose Better Query Plans]]></title>
            <link>https://camusdb.github.io/blog/dynamic-programming-query-optimizer/</link>
            <guid>https://camusdb.github.io/blog/dynamic-programming-query-optimizer/</guid>
            <pubDate>Sat, 11 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[One of the most interesting parts of building CamusDB is the query optimizer.]]></description>
            <content:encoded><![CDATA[<p>One of the most interesting parts of building CamusDB is the query optimizer.
It might also be one of the most complex pieces of machinery in the whole
database.</p>
<p>At first, a query optimizer sounds simple: receive a SQL query and decide how
to run it. But the more features a database supports, the harder that decision
becomes. A query can use a table scan, an index lookup, an index range scan, a
hash join, a merge join, a nested loop join, a sort, an aggregate, a derived
table, or a subquery. Each option can be correct, but not every option is fast.</p>
<p>The optimizer's job is to choose a good plan before the query runs.</p>
<p>That is a difficult job because SQL is declarative. When a user writes:</p>
<div class="language-camussql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-camussql codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token keyword" style="color:#00009f">SELECT</span><span class="token plain"> u</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">email</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"> p</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">title</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"> c</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">body</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">FROM</span><span class="token plain"> users u</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">JOIN</span><span class="token plain"> posts p </span><span class="token keyword" style="color:#00009f">ON</span><span class="token plain"> p</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">user_id </span><span class="token operator" style="color:#393A34">=</span><span class="token plain"> u</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">id</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">JOIN</span><span class="token plain"> comments c </span><span class="token keyword" style="color:#00009f">ON</span><span class="token plain"> c</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">post_id </span><span class="token operator" style="color:#393A34">=</span><span class="token plain"> p</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">id</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">WHERE</span><span class="token plain"> u</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">active </span><span class="token operator" style="color:#393A34">=</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">true</span><span class="token punctuation" style="color:#393A34">;</span><br></div></code></pre></div></div>
<p>they are not saying:</p>
<ol>
<li class="">Read <code>users</code> first.</li>
<li class="">Then join <code>posts</code>.</li>
<li class="">Then join <code>comments</code>.</li>
</ol>
<p>They are saying what result they want. The database must decide the how.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-join-order-matters">Why Join Order Matters<a href="https://camusdb.github.io/blog/dynamic-programming-query-optimizer/#why-join-order-matters" class="hash-link" aria-label="Direct link to Why Join Order Matters" title="Direct link to Why Join Order Matters" translate="no">​</a></h2>
<p>For small queries, the difference may not look important. For larger tables, it
can be huge.</p>
<p>Imagine this:</p>
<ul>
<li class=""><code>users</code> has many rows, but only a few are active.</li>
<li class=""><code>posts</code> has many rows for each user.</li>
<li class=""><code>comments</code> has many rows for each post.</li>
</ul>
<p>If CamusDB starts with all comments, then joins posts, then joins users, it may
touch a lot of data before discovering that most rows do not match
<code>u.active = true</code>.</p>
<p>If it starts with the filtered active users, it may reduce the problem much
earlier. The final result is the same, but the amount of work can be very
different.</p>
<p>This is where the optimizer becomes more than a parser or a set of simple
rules. It has to reason about cost.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-search-space-grows-fast">The Search Space Grows Fast<a href="https://camusdb.github.io/blog/dynamic-programming-query-optimizer/#the-search-space-grows-fast" class="hash-link" aria-label="Direct link to The Search Space Grows Fast" title="Direct link to The Search Space Grows Fast" translate="no">​</a></h2>
<p>The hard part is that join order has many possibilities.</p>
<p>With two tables, there are only a few choices. With three tables, there are
more. With five, six, or seven tables, the number of possible orders grows very
quickly.</p>
<p>A naive optimizer could try every possible order, estimate the cost of each
one, and pick the cheapest. That works for tiny cases, but it does not scale
well. The optimizer itself would become too slow.</p>
<p>This is a classic database problem. CamusDB now uses a System-R-style dynamic
programming approach for eligible inner joins when cost-based join ordering is
enabled.</p>
<p>The idea is simple enough to explain, even if the implementation has many
details.</p>
<p>Instead of asking "what is the best plan for the whole join?" immediately, the
optimizer asks smaller questions first:</p>
<ul>
<li class="">What is the best plan for <code>users</code>?</li>
<li class="">What is the best plan for <code>posts</code>?</li>
<li class="">What is the best plan for <code>comments</code>?</li>
<li class="">What is the best plan for <code>users JOIN posts</code>?</li>
<li class="">What is the best plan for <code>posts JOIN comments</code>?</li>
<li class="">Can those smaller answers help build the best larger answer?</li>
</ul>
<p>Dynamic programming is useful here because it remembers the best answer for a
smaller subset of tables and reuses it later. It avoids doing the same work
again and again.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-small-mental-model">A Small Mental Model<a href="https://camusdb.github.io/blog/dynamic-programming-query-optimizer/#a-small-mental-model" class="hash-link" aria-label="Direct link to A Small Mental Model" title="Direct link to A Small Mental Model" translate="no">​</a></h2>
<p>Think of the optimizer as filling a table of partial plans.</p>
<p>First, it estimates the best way to access each individual table. Maybe one
table should use an index. Maybe another should use a full scan.</p>
<p>Then it looks at pairs of tables that can be joined. For each pair, it asks:</p>
<ul>
<li class="">Can this pair be joined with an indexed nested loop?</li>
<li class="">Is a hash join cheaper?</li>
<li class="">Can both sides arrive ordered, making merge join possible?</li>
<li class="">How many rows will probably come out?</li>
<li class="">How expensive is the intermediate result?</li>
</ul>
<p>Then it does the same for three-table groups, then four-table groups, and so
on, always building from the best smaller plans it already found.</p>
<p>At the end, it has a candidate plan for the full query.</p>
<p>This does not mean the optimizer has perfect knowledge. It is still estimating.
But it is estimating in a structured way.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-more-technical-view">A More Technical View<a href="https://camusdb.github.io/blog/dynamic-programming-query-optimizer/#a-more-technical-view" class="hash-link" aria-label="Direct link to A More Technical View" title="Direct link to A More Technical View" translate="no">​</a></h2>
<p>The dynamic programming part works by keeping a memo table. In CamusDB, the
main idea is:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">dp[set of tables] = cheapest known left-deep plan for exactly that set</span><br></div></code></pre></div></div>
<p>For a query with three tables, the optimizer first creates entries for single
tables:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">dp[{users}]</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">dp[{posts}]</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">dp[{comments}]</span><br></div></code></pre></div></div>
<p>Each entry stores more than the table name. It stores a partial plan, the
estimated cost, and the estimated number of rows that the partial plan will
produce.</p>
<p>Then CamusDB tries to build larger entries from smaller ones:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">dp[{users, posts}]</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">dp[{posts, comments}]</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">dp[{users, comments}]</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">dp[{users, posts, comments}]</span><br></div></code></pre></div></div>
<p>The recurrence is roughly:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">best(S) =</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  min over each table R in S:</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">    best(S - R) joined with R</span><br></div></code></pre></div></div>
<p>There is one important rule: CamusDB only considers a join when there is a
connecting predicate between the existing left side and the new right table.
That prevents the optimizer from inventing cross products for normal joins.</p>
<p>For each possible extension, the optimizer asks which join shape is cheaper.
For example:</p>
<ul>
<li class="">If the right table has an index on the join key, an indexed nested-loop join
may be cheap.</li>
<li class="">If repeated index probes look expensive, a hash join may be cheaper.</li>
<li class="">If both sides can arrive ordered by the join key, a merge join can become a
good option.</li>
</ul>
<p>In simplified form, the optimizer is doing this:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">candidate_cost =</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  cost(best_left_plan)</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain">  + cost(join best_left_plan with next_table)</span><br></div></code></pre></div></div>
<p>If the new candidate is cheaper than the current <code>dp[S]</code>, it replaces it.</p>
<!-- -->
<p>The real implementation has more details, but this is the core shape.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-left-deep-plans">Why Left-Deep Plans?<a href="https://camusdb.github.io/blog/dynamic-programming-query-optimizer/#why-left-deep-plans" class="hash-link" aria-label="Direct link to Why Left-Deep Plans?" title="Direct link to Why Left-Deep Plans?" translate="no">​</a></h2>
<p>CamusDB currently searches left-deep join trees. A left-deep plan looks like
this:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">(((users JOIN posts) JOIN comments) JOIN reactions)</span><br></div></code></pre></div></div>
<p>The left side grows one table at a time, and each step adds one new table on
the right.</p>
<p>There are also bushy plans, where both sides of a join can already be joined
subtrees:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token plain">(users JOIN posts) JOIN (comments JOIN reactions)</span><br></div></code></pre></div></div>
<p>Bushy plans can be useful, but they make the search space larger. For now,
CamusDB keeps the search practical by using left-deep plans for this DP pass.
That is a common and useful step for a database optimizer.</p>
<p>The search also has boundaries:</p>
<ul>
<li class="">It is used for eligible inner joins.</li>
<li class="">It works with <code>JOIN ... ON</code> predicates that can be safely reordered.</li>
<li class="">It is capped for very wide joins, so planning stays bounded.</li>
<li class="">Unsupported shapes fall back to the heuristic join order.</li>
</ul>
<p>These limits are not failures. They are part of making the optimizer safe and
incremental.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="statistics-make-the-optimizer-smarter">Statistics Make The Optimizer Smarter<a href="https://camusdb.github.io/blog/dynamic-programming-query-optimizer/#statistics-make-the-optimizer-smarter" class="hash-link" aria-label="Direct link to Statistics Make The Optimizer Smarter" title="Direct link to Statistics Make The Optimizer Smarter" translate="no">​</a></h2>
<p>Dynamic programming gives CamusDB a better way to search. Statistics give it
better numbers to search with.</p>
<p>CamusDB can use table statistics, index counts, histograms, min/max values, and
distinct-value counts collected by <code>ANALYZE</code>. These values help answer
questions like:</p>
<ul>
<li class="">How many rows are in this table?</li>
<li class="">How selective is this filter?</li>
<li class="">Is this index likely to return a small number of rows?</li>
<li class="">How many rows might this join produce?</li>
</ul>
<p>For example:</p>
<div class="language-camussql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-camussql codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token keyword" style="color:#00009f">ANALYZE</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">TABLE</span><span class="token plain"> users</span><span class="token punctuation" style="color:#393A34">;</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">ANALYZE</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">TABLE</span><span class="token plain"> posts</span><span class="token punctuation" style="color:#393A34">;</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">ANALYZE</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">TABLE</span><span class="token plain"> comments</span><span class="token punctuation" style="color:#393A34">;</span><br></div></code></pre></div></div>
<p>After that, the optimizer has more information for cost estimates.</p>
<p>You can inspect the result with:</p>
<div class="language-camussql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-camussql codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token keyword" style="color:#00009f">EXPLAIN</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">SELECT</span><span class="token plain"> u</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">email</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"> p</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">title</span><span class="token punctuation" style="color:#393A34">,</span><span class="token plain"> c</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">body</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">FROM</span><span class="token plain"> users u</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">JOIN</span><span class="token plain"> posts p </span><span class="token keyword" style="color:#00009f">ON</span><span class="token plain"> p</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">user_id </span><span class="token operator" style="color:#393A34">=</span><span class="token plain"> u</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">id</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">JOIN</span><span class="token plain"> comments c </span><span class="token keyword" style="color:#00009f">ON</span><span class="token plain"> c</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">post_id </span><span class="token operator" style="color:#393A34">=</span><span class="token plain"> p</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">id</span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token keyword" style="color:#00009f">WHERE</span><span class="token plain"> u</span><span class="token punctuation" style="color:#393A34">.</span><span class="token plain">active </span><span class="token operator" style="color:#393A34">=</span><span class="token plain"> </span><span class="token keyword" style="color:#00009f">true</span><span class="token punctuation" style="color:#393A34">;</span><br></div></code></pre></div></div>
<p><code>EXPLAIN</code> shows the selected physical plan and the estimated rows and cost.
That makes the optimizer less mysterious. You can see whether CamusDB picked a
hash join, merge join, indexed nested loop join, or a fallback nested loop
join.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-this-is-hard-in-a-real-database">Why This Is Hard In A Real Database<a href="https://camusdb.github.io/blog/dynamic-programming-query-optimizer/#why-this-is-hard-in-a-real-database" class="hash-link" aria-label="Direct link to Why This Is Hard In A Real Database" title="Direct link to Why This Is Hard In A Real Database" translate="no">​</a></h2>
<p>The clean explanation is only part of the story.</p>
<p>In the real engine, the optimizer has to deal with many constraints:</p>
<ul>
<li class="">It must preserve SQL semantics.</li>
<li class="">It must respect join predicates.</li>
<li class="">It must not reorder joins when that would change the result.</li>
<li class="">It must understand which indexes are available.</li>
<li class="">It must estimate row counts from imperfect statistics.</li>
<li class="">It must choose between different join algorithms.</li>
<li class="">It must still produce a plan quickly.</li>
</ul>
<p>This is why the query optimizer is such complex machinery. It sits between the
friendly SQL that users write and the lower-level storage operations that the
database must execute.</p>
<p>When it works well, users should not have to think too much about it. They
write SQL, create useful indexes, run <code>ANALYZE</code> when data changes, and the
database does the best it can.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="keeping-the-system-practical">Keeping The System Practical<a href="https://camusdb.github.io/blog/dynamic-programming-query-optimizer/#keeping-the-system-practical" class="hash-link" aria-label="Direct link to Keeping The System Practical" title="Direct link to Keeping The System Practical" translate="no">​</a></h2>
<p>CamusDB still keeps the cost-based optimizer behind configuration flags:</p>
<div class="language-yaml codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#393A34;--prism-background-color:#f6f8fa"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-yaml codeBlock_bY9V thin-scrollbar" style="color:#393A34;background-color:#f6f8fa"><code class="codeBlockLines_e6Vv"><div class="token-line" style="color:#393A34"><span class="token key atrule" style="color:#00a4db">cost_based_access_path_enabled</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token boolean important" style="color:#36acaa">true</span><span class="token plain"></span><br></div><div class="token-line" style="color:#393A34"><span class="token plain"></span><span class="token key atrule" style="color:#00a4db">cost_based_join_order_enabled</span><span class="token punctuation" style="color:#393A34">:</span><span class="token plain"> </span><span class="token boolean important" style="color:#36acaa">true</span><br></div></code></pre></div></div>
<p>That is intentional. The rule-based planner remains stable and predictable.
The cost-based planner can be enabled when you want CamusDB to compare more
plans and use statistics to make better choices.</p>
<p>This is also useful while the database is still evolving. Query optimizers are
not finished in one pass. They improve over time, as more query shapes, more
statistics, and more execution strategies become available.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-i-like-this-feature">Why I Like This Feature<a href="https://camusdb.github.io/blog/dynamic-programming-query-optimizer/#why-i-like-this-feature" class="hash-link" aria-label="Direct link to Why I Like This Feature" title="Direct link to Why I Like This Feature" translate="no">​</a></h2>
<p>Dynamic programming is one of those ideas that feels elegant because it turns a
large problem into smaller reusable pieces.</p>
<p>It does not remove the complexity of query optimization. It gives the
complexity a shape.</p>
<p>That is exactly the kind of engineering I enjoy in CamusDB. The user sees a SQL
query. Underneath, the database is making many decisions: how to read, how to
join, how much data it expects, and which path is probably cheaper.</p>
<p>There is still a lot to improve, but this is an important step. CamusDB is
moving from "can this query run?" toward "can this query run with a plan that
makes sense?"</p>
<p>For a database, that is a big milestone.</p>]]></content:encoded>
            <category>camusdb</category>
            <category>query-optimizer</category>
            <category>sql</category>
        </item>
        <item>
            <title><![CDATA[Why CamusDB Exists]]></title>
            <link>https://camusdb.github.io/blog/welcome/</link>
            <guid>https://camusdb.github.io/blog/welcome/</guid>
            <pubDate>Fri, 03 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Perfection is achieved, not when there is nothing more to add, but when there is nothing left to take away.]]></description>
            <content:encoded><![CDATA[<blockquote>
<p>Perfection is achieved, not when there is nothing more to add, but when there is nothing left to take away.</p>
<p>— Antoine de Saint-Exupéry</p>
</blockquote>
<p>For many years I worked with Google Spanner, and somewhere along the way I fell in love with distributed SQL databases.</p>
<p>That may sound a little too dramatic for a database, but honestly, it is true.</p>
<p>In the video game industry, databases are not just boring infrastructure sitting in the background. They are a critical part of the player experience. Games need to handle spikes, global traffic, competitive systems, economies, inventories, matchmaking, leaderboards, live events, rewards, and a lot of things that must work correctly even when thousands or millions of players are active.</p>
<p>That is where distributed SQL really clicked for me.</p>
<p>Strong consistency matters. Scalability matters. Being able to reason about your data without constantly adding workarounds, queues, caches, compensating jobs, or manual repair scripts matters a lot. Spanner showed me that a database could solve many of these problems at the infrastructure level instead of forcing every backend team to reinvent the same patterns over and over again.</p>
<p>At some point, I started asking myself: what would it look like to build something like this myself?</p>
<p>Not because the world needs yet another database just for the sake of it, but because I wanted to explore the architecture deeply. I wanted to understand the tradeoffs, the hard parts, the abstractions, and the places where I could experiment with ideas that I think are useful.</p>
<p>That is how CamusDB started.</p>
<p>I began working on it in 2021. At the beginning, it felt like a big experiment. As I kept going, I realized it was not just big; it was a huge endeavor. Building a database, especially a distributed SQL database, touches almost every hard problem in backend infrastructure.</p>
<p>But that is also what makes it exciting.</p>
<p>It is challenging, sometimes painfully so, but I like that. I enjoy working on systems where every layer matters and where there is always one more thing to understand better.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-net">Why .NET?<a href="https://camusdb.github.io/blog/welcome/#why-net" class="hash-link" aria-label="Direct link to Why .NET?" title="Direct link to Why .NET?" translate="no">​</a></h2>
<p>One question I get often is: why build a database in .NET?</p>
<p>The short answer is: because .NET today is not the .NET people remember from 10 or 15 years ago.</p>
<p>The platform has been improving year after year, especially in performance. The JIT, the GC, the runtime, networking, memory APIs, async performance, Native AOT, and the standard libraries have all improved a lot. Modern .NET is fast, mature, productive, and very capable of handling serious backend infrastructure workloads.</p>
<p>I have been working with .NET for many years, and I know the ecosystem well. For me, building CamusDB in .NET is not a limitation. It is actually one of the reasons I can move fast while still keeping the system clean and understandable.</p>
<p>There is also something I like about challenging the default assumption that infrastructure projects must be written in Go, Rust, C++, or Java to be taken seriously. Those are great languages, of course, but .NET has earned its place in this conversation.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="camusdb-did-not-appear-out-of-nowhere">CamusDB did not appear out of nowhere<a href="https://camusdb.github.io/blog/welcome/#camusdb-did-not-appear-out-of-nowhere" class="hash-link" aria-label="Direct link to CamusDB did not appear out of nowhere" title="Direct link to CamusDB did not appear out of nowhere" translate="no">​</a></h2>
<p>CamusDB is not the first piece of this puzzle.</p>
<p>For years I have been building the lower-level infrastructure needed to make something like this possible.</p>
<p>The first big piece is <a href="https://github.com/kahunakv/kahuna" target="_blank" rel="noopener noreferrer" class="">Kahuna</a>, a distributed key/value and locking layer. Kahuna provides the coordination and transactional primitives that CamusDB uses as its foundation.</p>
<p>Then there is <a href="https://github.com/kahunakv/kommander" target="_blank" rel="noopener noreferrer" class="">Kommander</a>, the Raft library used to build the replicated consensus layer. Raft is one of those things that looks simple when you read the paper, but once you start implementing it and testing failure scenarios, you realize how many details matter.</p>
<p>CamusDB sits on top of all of that work.</p>
<p>So in many ways, CamusDB is the result of years of building the pieces underneath it: distributed coordination, locks, key/value storage, replication, consensus, transactions, and all the boring but necessary machinery that makes a database possible.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-build-my-own-database">Why build my own database?<a href="https://camusdb.github.io/blog/welcome/#why-build-my-own-database" class="hash-link" aria-label="Direct link to Why build my own database?" title="Direct link to Why build my own database?" translate="no">​</a></h2>
<p>A big reason is freedom.</p>
<p>When you work on your own database, you can add features that you personally believe are useful, even if they are not common yet in mainstream systems.</p>
<p>One example is database branching.</p>
<p>The idea is simple: what if branching a database felt more like branching code? What if you could create isolated branches of your data for testing, development, experiments, previews, or risky changes without needing to clone everything manually or maintain a bunch of fragile environments?</p>
<p>That is the kind of feature I find exciting because it comes from real-world pain. In backend and game development, we constantly need safe environments to test changes. We need to validate economy changes, content updates, migrations, live event configurations, and backend behavior without affecting production data.</p>
<p>Database branching is one of those features that feels obvious once you start thinking about databases as developer tools, not just storage engines.</p>
<p>And that is part of the philosophy behind CamusDB. I do not want to build only a technically interesting database. I want to build something useful. Something that solves practical problems for developers and teams.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-hard-parts-are-the-fun-parts">The hard parts are the fun parts<a href="https://camusdb.github.io/blog/welcome/#the-hard-parts-are-the-fun-parts" class="hash-link" aria-label="Direct link to The hard parts are the fun parts" title="Direct link to The hard parts are the fun parts" translate="no">​</a></h2>
<p>Building a distributed SQL database is not easy.</p>
<p>You have to think about query planning, transactions, distributed execution, indexes, consistency, replication, schema changes, locking, fault tolerance, storage, snapshots, and a long list of details that can break in subtle ways.</p>
<p>But that is also what makes it interesting.</p>
<p>Every feature forces you to understand the system more deeply. Every bug teaches you something. Every design decision has a tradeoff. You cannot hide from the complexity, but you can try to make it manageable and build the system piece by piece.</p>
<p>That process has been one of the most rewarding parts of working on CamusDB.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="where-things-are-now">Where things are now<a href="https://camusdb.github.io/blog/welcome/#where-things-are-now" class="hash-link" aria-label="Direct link to Where things are now" title="Direct link to Where things are now" translate="no">​</a></h2>
<p>The result is starting to look incredible.</p>
<p>CamusDB is no longer just an idea or a weekend experiment. It is becoming a real database with real capabilities, built on top of infrastructure that has taken years to design and implement.</p>
<p>There is still a lot to do. Databases are never "done." And honestly, the more I work on it, the more I understand how big the endeavor really is.</p>
<p>But that is part of the appeal.</p>
<p>It is hard, it is ambitious, and it constantly pushes me into problems that are deeper than they first look. That challenge is one of the main reasons I keep building it.</p>
<p>CamusDB started from my experience with Spanner, my love for distributed SQL, and years of dealing with backend problems in video games. It grew out of the belief that strong consistency and scalability should be more accessible, and that modern .NET is a great platform to build serious infrastructure.</p>
<p>But at the core, the reason is simple:</p>
<p>I wanted to build the database I wish I had for many of the systems I have worked on.</p>
<p>And so far, it has been one of the most exciting projects I have ever built.</p>]]></content:encoded>
            <category>camusdb</category>
        </item>
    </channel>
</rss>